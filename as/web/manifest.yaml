id: web-skill
path: skills/ai.luminarys.as.web.skill
permissions:
  http:
    enabled: true
    allowlist: ["**"]
invoke_policy:
  can_invoke: []
  can_be_invoked_by: ["*"]
mcp:
  mapping: per_method
