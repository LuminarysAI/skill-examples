id: archive-skill
path: skills/ai.luminarys.rust.archive.skill
permissions:
  fs:
    enabled: true
    dirs: ["/data:rw"]
invoke_policy:
  can_invoke: []
  can_be_invoked_by: ["*"]
mcp:
  mapping: per_method
