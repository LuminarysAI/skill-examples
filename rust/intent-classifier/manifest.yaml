id: intent-classifier
path: skills/ai.luminarys.rust.intent-classifier.skill
permissions:
  fs: { enabled: false }
invoke_policy:
  can_invoke: []
  can_be_invoked_by: ["*"]
mcp:
  mapping: per_method
