id: tree-sitter
path: skills/ai.luminarys.rust.tree-sitter.skill
permissions:
  fs:
    enabled: true
    dirs: ["/data:ro"]
invoke_policy:
  can_invoke: []
  can_be_invoked_by: ["*"]
mcp:
  mapping: per_method
